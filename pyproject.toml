[project]
name = "ligo-at"
version = "0.0.1"
description = "Decentralized links page on top of AT Protocol"
readme = "readme.md"
requires-python = ">=3.13"
dependencies = [
    "authlib>=1.3",
    "dnspython>=2.8.0",
    "flask[dotenv]>=3.1.2",
    "gunicorn>=23.0.0",
    "httpx>=0.28.1",
]
